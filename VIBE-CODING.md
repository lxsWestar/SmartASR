# Vibe Coding 通用开发规范 v2

> 适用范围：一切由 AI Agent 协作的软件工程项目
> 核心价值：**优质目录结构** · **单一事实来源** · **AI 可发现性** · **可复用技能体系**

---

## 0. 规则优先级

规则冲突时，按以下顺序优先：

1. **许可证与安全合规** — 不可逾越的红线
2. **项目级 AGENTS.md** — 项目特有的架构约束与命名规范
3. **本规范中的明确规则** — 下文各节
4. **常识与惯例** — 无明文规定时的缺省行为

---

## 1. 上下文加载协议（Context-First）

**在任何操作之前**，AI 必须按顺序读取以下文件（如存在）：

```
加载顺序:
  1. {project-root}/AGENTS.md          ← 全局 AI 协作规范与命名索引
  2. 当前目标模块的 README.md / claude.md  ← 局部上下文与隐式契约
  3. 目标模块的 agents/ 目录            ← 模块级 AI 专属指令
```

> **原则**：未读文档前不写代码。上下文加载不是可选步骤，是防止 AI 幻觉的第一道防线。

---

## 2. 迅捷开发原则（Quick Flow）

> *"能跑的代码胜过完美但不存在的代码。"*

### 2.1 最小仪式感

- **先让它跑起来**：优先可运行的最小实现，再迭代完善
- **文档够用即止**：Spec 是为了 Build，不是为了展示流程的完整性
- **增量落地**：每完成一个子任务立即保存产物，不积攒批量处理

### 2.2 完成标准（Done = Done）

一个任务被标记完成的充要条件：

```
✅ 实现代码到位
✅ 对应测试已编写
✅ 全部测试 100% 通过
✅ 相关文档 / File List 已更新
✅ 决策原因已记录（注释或 Dev Record）
```

### 2.3 进度追踪

复杂任务必须使用任务清单：
- 拆解为可独立验证的最小步骤
- 开始每步前标记 `in-progress`（同一时刻只能有一个 in-progress）
- 完成后**立即**标记 `completed`，不攒批
- 单步简单操作无需清单

### 2.4 并行与依赖

- 独立的只读操作（搜索、文件读取）可**并行调用**
- 有依赖关系的操作必须**串行**，等上一步结果再继续
- 永远不在缺少上一步结果的情况下猜测参数

---

## 3. 项目目录规范

### 3.1 顶层结构约定

```
{project-root}/
├── documents/          # 所有通用文档（API、设计、指南）
│   ├── api/
│   ├── design/
│   └── guides/
├── tests/              # 所有测试文件
│   └── {task-name}/    # 每个独立测试任务独占子目录
├── scripts/            # 运维、部署、工具脚本
│   └── {purpose}/      # 脚本用途明确时创建子目录（如 scripts/db/）
├── AGENTS.md           # AI 协作全局规范（SSOT）
└── README.md           # 项目入口说明
```

**就近原则**：脚本仅服务特定模块时 → 放该模块目录下，不污染根级 `scripts/`。

**严禁散乱**：禁止在项目根目录直接创建功能性脚本或文档文件。

### 3.2 每个文件夹必须有说明书

**每一个子目录 = `README.md`（说明）+ `agents/`（AI 指令骨架）**

```
{any-folder}/
├── README.md 或 claude.md    ← 单一事实来源（SSOT）
│     必须包含:
│       · 模块职责与设计原理
│       · 上游 / 下游依赖链
│       · 执行参数与依赖环境
│       · 隐式契约（性能约束、线程安全、禁止事项等）
└── agents/                   ← AI 专属指令目录（未来自动化预留）
    └── .gitkeep              ← 目录占位
```

> 没有说明书的目录 = AI 导航盲区。AI 在缺乏上下文时会凭借偏好填充逻辑，产生幻觉。

### 3.3 全局辅助文件

| 文件 | 用途 |
|------|------|
| `AGENTS.md` | AI 行为规范、命名索引、禁止操作、变量名大全 |
| `README.md`（根目录） | 项目目的、安装步骤、核心架构入口 |
| `TODO.md` / `TODO-*.md` | 开发计划与任务状态追踪 |

---

## 4. 代码质量约定

### 4.1 文件头注释（强制）

每个代码文件顶部必须包含：

```python
# ------------------------------------------------------------------
# 功能: <文件角色的简要描述>
# 维护者: <负责的 Agent 或开发者标识>
# ------------------------------------------------------------------
```

### 4.2 命名一致性

- 若 `AGENTS.md` 中存在**变量名索引表**，严格遵守，不得自造同义词
- 命名规范冲突时，以项目级 `AGENTS.md` 为准
- 新增命名必须同步更新索引表

### 4.3 代码规范基准

```yaml
异常处理:
  - 使用项目定义的异常类，不用裸 Exception
  - 禁止 print() 替代 logging（测试调试除外）

路径处理:
  - 使用 pathlib.Path，跨平台传参用 .as_posix()

API 优先:
  - 优先 API 交互，UI 是可选的解耦层
  - 任何新功能不假设成功，必须通过测试或 curl 验证

部署兼容:
  - 配置文件使用相对路径，兼容多环境（Windows / WSL2 / Linux）
```

---

## 5. AgentSkills 技能体系

Skills 是将领域知识封装为**可复用、可激活**的 AI 能力单元。

### 5.1 技能目录结构

```
skills/{skill-name}/
├── SKILL.md          ← 技能入口（触发词、边界、快速参考、示例）
├── references/       ← 长篇参考材料（从 SKILL.md 拆出，避免过长）
│   └── index.md      ← 参考材料导航索引
├── scripts/          ← 脚手架与工具脚本（可选）
└── assets/           ← 模板与静态资产（可选）
```

### 5.2 SKILL.md 标准结构

```markdown
---
name: skill-name
description: "一句话触发描述，必须含触发关键词"
---

## When to Use（触发条件）
- 明确的触发词列表
- 适用场景描述

## Not For / Boundaries（边界）
- 明确排除的场景（防止误触发）
- 与相邻技能的分工说明

## Quick Reference（核心速查）
- 最高频的操作模式
- 可复制的代码 / 命令示例

## Examples（可复现示例）
- 具体场景 → 具体输出的对应关系
```

**关键约束**：
- SKILL.md 长度超过 300 行时，必须将长篇内容拆入 `references/`
- AI 激活前必须读取完整 SKILL.md，不可猜测技能内容
- 若材料不支持某声明，明确说明并提供验证路径，不伪造依据
- 多个 Skill 可叠加使用（如 `copywriting` + `analytics-tracking`）

### 5.3 新建技能检查清单

```
□ SKILL.md 包含 frontmatter（name + description）
□ 触发词明确，边界清晰，不与现有 Skill 重叠
□ Quick Reference 含可复现示例
□ 长篇内容已拆入 references/，references/index.md 已创建
□ 技能注册到全局技能目录（如 .copilot/skills/ 下的索引）
```

---

## 6. 快速备忘

| 场景 | 动作 |
|------|------|
| 找不到某模块的用途 | 读该目录下的 `README.md` 或 `claude.md` |
| 变量 / 类名命名疑问 | 先查 `AGENTS.md` 变量名索引 |
| 测试放在哪里 | `tests/{task-name}/` 独立子目录 |
| 脚本放在哪里 | `scripts/{purpose}/` + 配套 `README.md` |
| 新增技能 | 在 `skills/{name}/` 下建 `SKILL.md`，填标准结构 |
| AI 产生了幻觉 | 检查目标目录是否有 `README.md` / `claude.md` |
| 任务很复杂 | 拆解任务清单，逐步标记 in-progress → completed |
| 新建一个目录 | 同步创建 `README.md` + `agents/.gitkeep` |
